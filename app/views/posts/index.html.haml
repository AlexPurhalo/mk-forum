- if user_signed_in?
  %p#intro
    Hello
    = current_user.name
    %br/
    %span
      Share your inspiration and see what's inspiring others.
- else
  %p#intro
    What's your muse?
    %br/
    %span
      Share your inspiration and see what's inspiring others.

#posts
  - @posts.each do |post|
    .post
      = link_to(post) do
        .post_image
          = image_tag post.image.url(:small)
        .post_content
          .title
            %h2= post.title
          .data.clearfix
            %p.username
              Shared by
              = post.user.name
            %p.buttons
              %span
                %i.fa-comments-o
                = post.comments.count
              %span
                %i.fa-comments-o-up
                = post.get_likes.size


